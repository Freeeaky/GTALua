; Call-Layout: [parameters] ) [return values]
;
; Types [case sensitive!]:
;   i = int / number
;   f = float / number
;   b = bool
;   v = void ( = no return value)
;   V = vector / Vector
;   m = CMemoryBlock
;
;   a/u = any/unknown -> interpreted as int
;
; You can call natives with call layouts like this:
;     natives.[category].[name]([parameters])